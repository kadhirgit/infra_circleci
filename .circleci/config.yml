version: 2.1

jobs:
  create_infrastructure:
    docker:
      - image: amazon/aws-cli
    steps:
      - add_ssh_keys:
          fingerprints:
            - "46:6e:23:f0:00:75:c4:3b:11:a6:f4:46:3f:41:47:97"
      - checkout
      - run:
          name: Ensure backend infrastructure exist
          command: |
            aws cloudformation deploy \
              --template-file template.yml \
              --stack-name my-stack
workflows:
  my_workflow:
    jobs:

      - create_infrastructure